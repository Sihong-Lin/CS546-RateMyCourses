const courseData = require('./course');
const professorData = require('./professor')
const userData = require('./user')

module.exports = {
    course: courseData,
    professor: professorData,
    user: userData
};
